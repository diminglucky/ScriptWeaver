# 知乎发布功能使用指南

## 📦 安装依赖

### 1. 安装 Playwright

```bash
pip install playwright
```

### 2. 安装浏览器驱动

```bash
playwright install chromium
```

> 注意：安装浏览器驱动会下载约 120MB 的 Chromium 浏览器

## 🚀 使用方法

### 1. 生成故事

在「故事生成」页面，按照正常流程生成完整的故事内容。

### 2. 生成标题

在故事内容下方的「发布到知乎」区域：
- 可以手动输入文章标题
- 或点击「🤖 AI生成标题」自动生成吸引人的标题

### 3. 发布到知乎

点击「📤 发布到知乎」按钮：

1. **首次使用**：会打开浏览器，需要手动登录知乎账号
   - 登录状态会被保存，下次使用无需重复登录
   
2. **自动填充**：脚本会自动：
   - 打开知乎创作页面
   - 进入文章编辑器
   - 填写标题和内容
   
3. **手动检查**：内容填充后，您可以：
   - 检查标题和内容格式
   - 添加封面图片
   - 选择话题标签
   - 点击「发布」按钮

4. **发布完成**：发布成功后，文章链接会自动复制到剪贴板

## ⚙️ 选项说明

- **后台运行（不显示浏览器）**：
  - 勾选：浏览器在后台运行，看不到界面（推荐熟练用户）
  - 不勾选：显示浏览器界面，可以看到整个过程（推荐首次使用）

## 🔒 安全说明

- 登录信息保存在本地：`~/.zhihu_publisher/` 目录
- 不会上传任何账号信息到服务器
- 使用 Playwright 官方安全机制

## ❓ 常见问题

### Q: 为什么需要手动登录？
A: 知乎使用了复杂的登录验证（验证码、滑块等），自动化登录容易被检测，因此采用手动登录更安全可靠。

### Q: 登录信息会过期吗？
A: 知乎的登录状态通常可以保持较长时间，如果过期会提示重新登录。

### Q: 可以一次发布多篇吗？
A: 目前每次发布一篇，发布成功后可以继续发布下一篇。

### Q: 发布失败怎么办？
A: 
1. 检查网络连接
2. 确认已登录知乎
3. 查看控制台错误信息
4. 尝试不勾选「后台运行」，观察浏览器中的操作

## 🛠️ 技术细节

- 使用 Playwright 作为浏览器自动化工具
- 支持登录状态持久化
- 异步操作，不阻塞UI
- 完善的错误处理和进度反馈

## 📝 代码结构

```
src/
├── services/
│   └── zhihu_publisher.py          # 知乎发布核心服务
└── gui/
    └── mixins/
        └── story_modules/
            └── zhihu_publisher_mixin.py  # UI集成Mixin
```

## 🎯 未来优化方向

- [ ] 支持添加封面图片
- [ ] 支持自动选择话题标签
- [ ] 支持草稿保存
- [ ] 支持定时发布
- [ ] 支持多账号管理

