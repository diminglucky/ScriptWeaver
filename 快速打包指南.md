# 🚀 快速打包指南

这是一个超简单的打包指南，帮你快速将程序打包成Windows可执行文件。

## 第一步：准备环境（仅需一次）

### 在Windows系统上：

1. **确认Python已安装**
   - 打开命令提示符（CMD）
   - 输入 `python --version`
   - 应该显示Python 3.10或更高版本

2. **安装PyInstaller**
   ```bash
   pip install pyinstaller
   ```

3. **安装项目依赖**
   ```bash
   cd 你的项目路径
   pip install -r requirements.txt
   ```

## 第二步：开始打包

### 方法1：使用打包脚本（最简单）⭐

双击运行 `打包脚本.bat`，然后：
- 输入 `1` 选择文件夹版本（推荐）
- 或输入 `2` 选择单文件版本

等待打包完成！

### 方法2：使用命令行

打开命令提示符，进入项目目录：

**打包文件夹版本（推荐）**：
```bash
pyinstaller build_windows.spec
```

**打包单文件版本**：
```bash
pyinstaller build_simple.spec
```

## 第三步：测试程序

打包完成后，打开 `dist` 文件夹：

- **文件夹版本**：`dist/AI故事创作工具/AI故事创作工具.exe`
- **单文件版本**：`dist/AI故事创作工具.exe`

双击运行，测试是否正常工作。

## 第四步：分发给用户

### 文件夹版本
将整个 `dist/AI故事创作工具/` 文件夹打包成zip，分发给用户：
```
AI故事创作工具_Windows版.zip
├── AI故事创作工具/        （整个文件夹）
├── 使用说明.txt
├── .env.example
└── data/raw/              （空文件夹，用户放素材）
```

### 单文件版本
直接将 `AI故事创作工具.exe` 和使用说明打包：
```
AI故事创作工具_Windows版.zip
├── AI故事创作工具.exe
├── 使用说明.txt
└── .env.example
```

## 常见问题

### ❓ 打包时报错 "No module named XXX"

**解决**：在命令行运行
```bash
pip install XXX
```
然后重新打包。

### ❓ 打包后程序无法运行

**解决**：
1. 修改 `build_windows.spec` 文件
2. 找到 `console=False` 改为 `console=True`
3. 重新打包，运行程序看错误提示

### ❓ 文件太大（超过500MB）

**这是正常的！** 因为包含了：
- Python运行环境
- 所有依赖库（包括AI模型加载器）
- 你的程序代码

如果想减小体积：
1. 使用文件夹版本（比单文件版本小）
2. 使用7zip压缩后分发

### ❓ 我用的是Mac/Linux系统

你需要：
1. 使用Windows虚拟机（VirtualBox、Parallels等）
2. 或者找一台Windows电脑进行打包
3. 或者使用Docker（较复杂，见《打包说明.md》）

⚠️ **重要**：PyInstaller必须在目标系统上运行，Mac上打包只能生成Mac版本。

## 进阶技巧

### 添加程序图标

1. 准备一个 `.ico` 图标文件（256x256或更小）
2. 将图标文件放到项目根目录，命名为 `icon.ico`
3. 修改 `build_windows.spec` 文件：
   ```python
   exe = EXE(
       ...,
       icon='icon.ico',  # 添加这一行
   )
   ```
4. 重新打包

### 减小打包体积

1. **使用虚拟环境**（推荐）：
   ```bash
   # 创建虚拟环境
   python -m venv venv_build
   
   # 激活虚拟环境
   venv_build\Scripts\activate
   
   # 只安装必需的包
   pip install -r requirements.txt
   pip install pyinstaller
   
   # 打包
   pyinstaller build_windows.spec
   ```

2. **排除不需要的库**：
   编辑 `build_windows.spec`，在 `excludes` 列表中添加不需要的模块。

### 清理构建文件

打包后会生成一些临时文件，运行：
```bash
# 删除临时文件
rmdir /s /q build
rmdir /s /q __pycache__
```

或者使用打包脚本选择选项 `3`。

## 完整流程示例

```bash
# 1. 克隆或打开项目
cd C:\Users\YourName\创作知乎故事

# 2. 安装依赖（仅首次）
pip install -r requirements.txt
pip install pyinstaller

# 3. 打包
pyinstaller build_windows.spec --clean

# 4. 测试
cd dist\AI故事创作工具
AI故事创作工具.exe

# 5. 如果正常，打包分发
cd ..\..
7z a AI故事创作工具_Windows版.zip dist\AI故事创作工具 使用说明.txt .env.example
```

## 需要帮助？

- 📖 详细说明：查看《打包说明.md》
- 🐛 遇到问题：在spec文件中设置 `console=True` 查看错误
- 💬 技术支持：[您的联系方式]

---

**祝打包顺利！** 🎉

如果一切正常，整个过程只需要5-10分钟！

