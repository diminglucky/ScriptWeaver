# Windows 打包说明

本文档说明如何将"AI故事创作工具"打包成Windows可执行程序（.exe文件）。

## 📋 准备工作

### 1. 环境要求
- **Python版本**: 3.10 或更高
- **操作系统**: Windows 10/11（用于打包）或使用虚拟机/交叉编译
- **磁盘空间**: 至少3GB可用空间（用于依赖和打包文件）

### 2. 安装PyInstaller
```bash
pip install pyinstaller
```

如果已有requirements.txt，建议先安装所有依赖：
```bash
pip install -r requirements.txt
pip install pyinstaller
```

## 🚀 打包步骤

### 方法一：生成文件夹版本（推荐）

这种方式会生成一个包含exe和依赖文件的文件夹，启动速度快。

```bash
pyinstaller build_windows.spec
```

打包完成后，可执行文件位于：
```
dist/AI故事创作工具/AI故事创作工具.exe
```

**优点**：
- 启动速度快
- 文件体积相对小
- 便于调试

**使用方法**：
将整个 `dist/AI故事创作工具/` 文件夹分发给用户，双击运行 `AI故事创作工具.exe`

---

### 方法二：生成单文件版本

这种方式会生成一个独立的exe文件，更方便分发但启动稍慢。

```bash
pyinstaller build_simple.spec
```

打包完成后，可执行文件位于：
```
dist/AI故事创作工具.exe
```

**优点**：
- 单个文件，方便分发
- 不需要担心文件夹结构

**缺点**：
- 文件体积大（可能300MB+）
- 首次启动需要解压，速度较慢

---

### 方法三：快速打包（不使用spec文件）

如果不想使用spec配置文件，也可以直接命令行打包：

#### 文件夹版本
```bash
pyinstaller --name="AI故事创作工具" ^
    --windowed ^
    --add-data="custom_api_presets.json;." ^
    --add-data="custom_image_api_presets.json;." ^
    --add-data="src;src" ^
    --add-data="docs;docs" ^
    --hidden-import="src.gui.modern_app" ^
    --hidden-import="src.gui_app" ^
    --hidden-import="tkinter" ^
    --hidden-import="PIL" ^
    run_modern_app.py
```

#### 单文件版本
```bash
pyinstaller --name="AI故事创作工具" ^
    --onefile ^
    --windowed ^
    --add-data="custom_api_presets.json;." ^
    --add-data="custom_image_api_presets.json;." ^
    --add-data="src;src" ^
    --add-data="docs;docs" ^
    --hidden-import="src.gui.modern_app" ^
    run_modern_app.py
```

**注意**: 
- Windows命令行使用 `^` 作为续行符
- Linux/Mac使用 `\` 作为续行符
- `--add-data` 参数在Windows上使用 `;` 分隔，Linux/Mac使用 `:` 分隔

## 📦 打包后的文件结构

### 文件夹版本
```
dist/
└── AI故事创作工具/
    ├── AI故事创作工具.exe          # 主程序
    ├── python311.dll               # Python运行时
    ├── _internal/                  # 依赖库和资源
    │   ├── src/                    # 源代码
    │   ├── docs/                   # 文档
    │   ├── custom_api_presets.json
    │   └── ...其他依赖
    └── ...其他DLL文件
```

### 单文件版本
```
dist/
└── AI故事创作工具.exe              # 独立的可执行文件
```

## 🔧 常见问题及解决方案

### 1. 打包时出现 "module not found" 错误

**原因**: PyInstaller没有自动检测到某些模块

**解决方法**: 
在spec文件的 `hiddenimports` 列表中添加缺失的模块，例如：
```python
hiddenimports=[
    'tkinter',
    'tkinter.ttk',
    'src.gui.modern_app',
    # 添加缺失的模块
    'missing_module_name',
],
```

### 2. 运行exe时出现 "Failed to execute script" 错误

**排查方法**:
1. 使用控制台模式重新打包，查看详细错误信息：
   ```bash
   # 在spec文件中设置 console=True
   exe = EXE(..., console=True, ...)
   ```

2. 或者直接命令行运行exe，查看输出信息

### 3. 打包后文件体积过大

**解决方法**:

1. **排除不必要的库**: 在spec文件中添加排除项
   ```python
   excludes=[
       'matplotlib',  # 如果不需要matplotlib
       'scipy',       # 如果不需要scipy
       'pandas',      # 如果不需要pandas
       'jupyter',
   ],
   ```

2. **使用UPX压缩**: PyInstaller默认会使用UPX压缩（如果可用）
   ```bash
   # 下载UPX: https://github.com/upx/upx/releases
   # 将upx.exe放到系统PATH中
   ```

3. **使用虚拟环境**: 只安装必需的包
   ```bash
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements.txt
   pip install pyinstaller
   pyinstaller build_windows.spec
   ```

### 4. 运行时找不到数据文件

**原因**: 程序中使用了相对路径，打包后路径发生变化

**解决方法**: 
确保程序中使用正确的路径获取方式：
```python
import sys
from pathlib import Path

# 获取程序运行目录
if getattr(sys, 'frozen', False):
    # 如果是打包后的程序
    application_path = Path(sys._MEIPASS)
else:
    # 如果是开发环境
    application_path = Path(__file__).parent

# 使用绝对路径访问资源
config_file = application_path / "custom_api_presets.json"
```

### 5. sentence-transformers模型下载问题

**问题**: 首次运行需要下载模型文件，但用户环境可能没有网络

**解决方法**:
1. 预先下载模型到本地
2. 将模型文件打包到程序中
3. 在文档中说明首次运行需要联网

### 6. 在Mac/Linux上为Windows打包

如果你使用的是Mac或Linux系统，需要：

**方法1: 使用虚拟机**
- 安装Windows虚拟机（VirtualBox、Parallels等）
- 在虚拟机中安装Python和依赖
- 在虚拟机中运行打包命令

**方法2: 使用Docker**
```bash
# 使用Windows容器进行打包
docker run -v "$(pwd):/src" cdrx/pyinstaller-windows
```

**方法3: 使用Wine（不推荐）**
- 安装Wine
- 通过Wine运行Windows版本的Python和PyInstaller

## 📝 分发清单

打包完成后，建议创建以下分发包：

```
AI故事创作工具_Windows版.zip
├── AI故事创作工具/              # 程序文件夹
│   ├── AI故事创作工具.exe
│   └── ...其他文件
├── 使用说明.txt                 # 简要使用说明
├── data/                        # 空的数据目录
│   └── raw/                     # 用户放置文本文件
├── projects/                    # 空的项目目录
│   └── .gitkeep
└── .env.example                 # 配置示例文件
```

### 使用说明.txt 示例内容：

```text
================================
   AI故事创作工具 - 使用说明
================================

欢迎使用AI故事创作工具！

【快速开始】
1. 双击运行"AI故事创作工具.exe"
2. 首次使用需要配置API密钥：
   - 在界面中填写您的DeepSeek API Key
   - 点击"保存配置"按钮
3. 在data/raw/文件夹中放入您的文本素材
4. 点击"构建索引"建立知识库
5. 开始创作您的故事！

【系统要求】
- Windows 10/11 (64位)
- 至少4GB内存
- 首次运行需要联网（下载AI模型）

【注意事项】
- 首次启动可能需要较长时间
- 程序会在用户目录下载AI模型文件
- 请确保有足够的磁盘空间（建议至少2GB）

【获取帮助】
- 查看docs文件夹中的详细文档
- 问题反馈：[您的联系方式]

================================
```

## ✅ 测试打包结果

打包完成后，建议进行以下测试：

1. **基础测试**
   - [ ] 程序能正常启动
   - [ ] 界面显示正常
   - [ ] 能够打开和保存文件

2. **功能测试**
   - [ ] 能够构建知识库索引
   - [ ] 能够生成故事目录
   - [ ] 能够生成完整故事
   - [ ] 能够保存项目

3. **环境测试**
   - [ ] 在纯净的Windows系统上测试（未安装Python）
   - [ ] 在不同版本Windows上测试（Win10/Win11）
   - [ ] 在没有网络的环境测试基本功能

## 🎯 优化建议

### 1. 添加程序图标

创建一个 `.ico` 图标文件，在spec文件中指定：
```python
exe = EXE(
    ...,
    icon='icon.ico',  # 图标文件路径
)
```

### 2. 添加版本信息

创建 `version_info.txt`:
```text
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 0, 0, 0),
    prodvers=(1, 0, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'080404b0',
        [StringStruct(u'CompanyName', u'Your Company'),
        StringStruct(u'FileDescription', u'AI故事创作工具'),
        StringStruct(u'FileVersion', u'1.0.0.0'),
        StringStruct(u'InternalName', u'AI故事创作工具'),
        StringStruct(u'LegalCopyright', u'Copyright (c) 2025'),
        StringStruct(u'OriginalFilename', u'AI故事创作工具.exe'),
        StringStruct(u'ProductName', u'AI Story Creator'),
        StringStruct(u'ProductVersion', u'1.0.0.0')])
      ]), 
    VarFileInfo([VarStruct(u'Translation', [2052, 1200])])
  ]
)
```

在spec文件中引用：
```python
exe = EXE(
    ...,
    version='version_info.txt',
)
```

### 3. 创建安装程序

使用 Inno Setup 或 NSIS 创建专业的安装程序，可以：
- 创建桌面快捷方式
- 添加到开始菜单
- 关联文件类型
- 支持卸载功能

## 📚 参考资源

- [PyInstaller官方文档](https://pyinstaller.org/en/stable/)
- [PyInstaller常见问题](https://github.com/pyinstaller/pyinstaller/wiki)
- [Python打包最佳实践](https://packaging.python.org/)

## 🆘 需要帮助？

如果遇到问题，请检查：
1. Python版本是否正确
2. 所有依赖是否安装完整
3. spec文件配置是否正确
4. 查看PyInstaller的详细日志输出

祝打包顺利！🎉

