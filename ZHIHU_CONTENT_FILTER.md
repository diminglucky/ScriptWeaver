# 知乎发布 - 内容智能过滤

## 📝 功能说明

在发布到知乎时，系统会**自动过滤**掉目录、章节标题等结构性信息，只保留纯净的故事正文内容。

## ✨ 过滤内容

### **自动过滤的内容：**

1. **目录部分**
   ```
   目录：
   1. 校园霸凌日常
   2. 职场初体验
   3. 危机与觉醒
   4. 蜕变与救赎
   ```

2. **章节标题**
   ```
   【第 1/4 章. 1. 校园霸凌日常】
   第一章 校园霸凌日常
   ==================
   ```

3. **分隔线**
   ```
   ====================
   --------------------
   ____________________
   ```

4. **短标题行**
   ```
   1. 校园霸凌日常
   2. 职场初体验
   ```

### **保留的内容：**

✅ 故事正文  
✅ 对话内容  
✅ 场景描述  
✅ 人物描写  
✅ 正常的段落间隔  

## 🎯 使用流程

### **1. 生成故事**
在「故事生成」页面生成完整故事（包含目录和章节）

### **2. 生成标题**
- 点击「🤖 AI生成标题」
- AI会基于**纯净的故事内容**生成标题（自动过滤目录）

### **3. 预览发布内容**
点击「📤 发布到知乎」后，会弹出预览窗口：

```
┌─────────────────────────────────────────┐
│  发布预览 - 确认内容                      │
├─────────────────────────────────────────┤
│  标题: 一个感人的成长故事                 │
│                                          │
│  📄 将要发布的内容（已自动过滤目录...）   │
│  ┌──────────────────────────────────┐  │
│  │ [纯净的故事正文]                  │  │
│  │                                   │  │
│  │ 清晨的阳光透过教室的窗户...         │  │
│  │                                   │  │
│  └──────────────────────────────────┘  │
│                                          │
│  原始: 3500字 → 过滤后: 3200字 | 已过滤 300字 │
│                                          │
│  [✅ 确认发布]  [❌ 取消]                │
└─────────────────────────────────────────┘
```

### **4. 确认发布**
- 在预览窗口中检查内容
- 确认无误后点击「✅ 确认发布」
- 系统会自动填充标题和纯净内容到知乎编辑器

### **5. 手动发布**
- 在知乎编辑器中可以：
  - 调整格式
  - 添加封面图片
  - 选择话题标签
  - 设置发布权限
- 检查无误后点击「发布」按钮

## 🔧 技术实现

### **智能过滤算法**

```python
class StoryExtractor:
    """故事内容提取器"""
    
    @staticmethod
    def extract_pure_story(full_text: str) -> str:
        """
        提取纯净的故事正文
        
        过滤规则：
        1. 识别并跳过目录部分
        2. 过滤章节标题（如：【第 1/4 章...】）
        3. 过滤短标题行（如：1. 校园霸凌日常）
        4. 过滤分隔线
        5. 保留故事正文段落
        6. 压缩多余空行
        """
```

### **过滤规则详解**

#### **1. 目录识别**
```python
# 识别 "目录：" 开始的部分
if re.match(r'^目录[：:]*\s*$', line):
    skip_toc = True

# 识别目录项（数字+短文本）
if re.match(r'^\d+[\.、]\s*.{1,30}$', line):
    continue  # 跳过
```

#### **2. 章节标题过滤**
```python
# 过滤【第 X/Y 章...】格式
if re.match(r'^【第\s*\d+/\d+\s*章.*】$', line):
    continue

# 过滤"第X章"格式
if re.match(r'^第[一二三四五六七八九十\d]+[章节篇]', line):
    continue
```

#### **3. 分隔线过滤**
```python
# 过滤连续的 =、-、_ 符号
if re.match(r'^[=\-_]{3,}$', line):
    continue
```

#### **4. 空行压缩**
```python
# 将多个连续空行压缩为一个
result = re.sub(r'\n{3,}', '\n\n', result)
```

## 📊 效果对比

### **原始内容（3500字）**
```
目录：
1. 校园霸凌日常
2. 职场初体验
3. 危机与觉醒
4. 蜕变与救赎

====================
【第 1/4 章. 1. 校园霸凌日常】

清晨的阳光透过教室的窗户，在地板上投下斑驳的光影...
[故事正文...]

【第 2/4 章. 2. 职场初体验】

毕业后的第一份工作...
[故事正文...]
```

### **过滤后（3200字）**
```
清晨的阳光透过教室的窗户，在地板上投下斑驳的光影...
[故事正文...]

毕业后的第一份工作...
[故事正文...]
```

**过滤掉：** 300字（目录、章节标题、分隔线）  
**保留：** 3200字（纯净故事正文）

## 💡 使用建议

1. **生成故事时**可以包含目录和章节标题（便于阅读和组织）
2. **发布到知乎时**会自动过滤（保持内容简洁）
3. **预览窗口**中可以看到过滤后的效果
4. **如果需要保留某些内容**，可以在预览后手动在知乎编辑器中添加

## ⚙️ 自定义过滤规则

如需修改过滤规则，可以编辑：
```
src/utils/story_extractor.py
```

例如：
- 调整目录识别模式
- 修改章节标题格式
- 自定义保留/过滤规则

---

**更新时间**: 2025-10-26  
**状态**: ✅ 已实现并测试

