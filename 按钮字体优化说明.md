# 按钮字体优化说明

## 修改内容

### 1. 图片生成页面按钮优化 (`src/gui/mixins/image_modules/ui_create.py`)

#### 分镜头按钮
- ✅ **智能推荐**: width 10 → 14
- ✅ **简短(8-12秒)**: width 11 → 16（文本改为"简短(8-12秒)"）
- ✅ **平衡(15-25秒)**: width 12 → 17（文本改为"平衡(15-25秒)"）
- ✅ **标准(15-22秒)**: width 12 → 17（文本改为"标准(15-22秒)"）
- ✅ **精细(25-40秒)**: width 12 → 17（文本改为"精细(25-40秒)"）

#### 图片描述按钮
- ✅ **从当前分镜生成**: width 18 → 20
- ✅ **复制**: width 8 → 10
- ✅ **清空**: width 8 → 10

#### 视频提示词按钮
- ✅ **复制视频提示词**: width 20 → 22

#### 操作按钮
- ✅ **生成图片**: 改用grid布局，自适应宽度
- ✅ **保存**: 改用grid布局，自适应宽度

### 2. 全局按钮样式优化 (`src/gui/modern_app.py`)

#### ttk.Button 样式
- ✅ **padding**: [24, 12] → [28, 14]（增加内边距）
- ✅ **font size**: 13 → 12（调整字体大小以适应中文+emoji）

## 优化效果

### Before（优化前）
- 按钮文字经常被遮挡
- emoji和中文挤在一起
- 视觉上不美观

### After（优化后）
- ✅ 所有按钮文字完整显示
- ✅ emoji和文字之间有合适间距
- ✅ 按钮更大更易点击
- ✅ 整体视觉效果更专业

## 技术说明

### Width 参数说明
在 tkinter 的 ttk.Button 中，`width` 参数的单位是**字符宽度**，不是像素：
- 1个英文字符 ≈ 1 width
- 1个中文字符 ≈ 2 width
- 1个emoji ≈ 2-3 width

### 计算公式
```python
# 例如：🎬 简短(8-12秒)
# emoji(3) + 空格(1) + 简短(4) + 括号和数字(7) = 约15
# 所以设置 width=16-17 比较合适
```

### Grid vs Pack
对于需要自适应宽度的按钮（如"生成图片"和"保存"），改用 `grid` 布局配合 `sticky="ew"` 和 `weight=1`，让按钮自动填充可用空间。

## 其他建议

### 如果还有按钮显示问题
1. 检查 `width` 参数是否足够大
2. 检查是否有特殊字符或emoji占用额外空间
3. 尝试增加 `padding` 参数
4. 考虑使用 `grid` 布局代替 `pack`

### 测试方法
```bash
python run_modern_app.py
```

然后：
1. 切换到【图片生成 → 创作】标签页
2. 检查所有按钮文字是否完整显示
3. 测试按钮点击功能是否正常

## 文件清单

修改的文件：
- ✅ `src/gui/mixins/image_modules/ui_create.py`
- ✅ `src/gui/modern_app.py`

## 版本信息

- 修改日期: 2025-10-19
- 修改原因: 用户反馈按钮文字被遮挡
- 测试状态: ⏳ 待用户测试

